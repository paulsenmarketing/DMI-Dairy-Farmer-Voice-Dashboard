
{% import '_templates/elements/image.twig' as Image  %}

{% set images = block.images.eagerly() %}
<div class="w-full grid grid-cols-2 gap-0">
   {% for image in images %}
      <div class="max-h-[400px] w-full">
         {{ Image.Default(image) }}
      </div>
   {% endfor %}
</div>