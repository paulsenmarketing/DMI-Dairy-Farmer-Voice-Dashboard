{% import '_templates/elements/button.twig' as Button %}
{% import '_templates/elements/logo.twig' as Logo %}
{% set image = siteSettings.siteLogo.one() %}

{% set navNodes = craft.navigation.nodes().handle('default').level(1).all() %}


    <div class="w-full px-5 bg-brandWhiteBlue py-12 lg:py-16">
        <div class="block-container">
            <div class="w-full flex flex-col md:flex-row justify-between lg:items-center mb-12">
                <div class="flex flex-col sm:flex-row md:flex-col justify-between items-start lg:w-fit space-y-6 mb-8">
                    <a href="/">
                        <img class="h-40" src="{{ image.url }}">
                    </a>
                    <p class="max-w-80 !mt-0">Lorem ipsum dolor sit amet, consec tetur adipisicing elit, sed do eiusmod tempor incididuntut consec.</p>
                </div>
{#                <div class="flex flex-wrap gap-8 justify-end">#}
{#                    {{ Button.social('linkedin') }}#}
{#                    {{ Button.social('youtube') }}#}
{#                    {{ Button.social('facebook') }}#}
{#                    {{ Button.social('x') }}#}
{#                    {{ Button.social('instagram') }}#}
{#                </div>#}
                <div class="space-y-6 lg:w-fit">
                    <div class="flex flex-col">
                        <h3 class="border-b-2 w-48 border-black pb-2 font-bold">Quick Links</h3>
                        {% for node in navNodes %}
                            {% if node.hasDescendants() %}
                                {{ macros.navNodes(node) }}
                            {% else %}
                                <a href="{{ node.url }}" class="py-2 text-black hover:underline transition-colors duration-200 w-max" >
                                    {{ node.title }}
                                </a>
                            {% endif %}
                        {% endfor %}
                    </div>
                </div>
                <div class="lg:w-fit flex flex-col gap-4">
                    <div>
                        <h3 class="font-bold">Find Us</h3>
                        <p>1234 N. Street St.</p>
                        <p>City, State, Country</p>
                    </div>
                    <div>
                        <h3 class="font-bold">Call Us</h3>
                        <p>(123) 456-7890</p>
                    </div>
                    <div>
                        <h3 class="font-bold">Send Us a Message</h3>
                        <p>company@webserver.com</p>
                    </div>
                </div>
            </div>

            <div class="w-full flex flex-col justify-center items-center">
                <p>©{{ 'now' | date('Y') }} Dairy Farmer Voice Dashboard | All Rights Reserved • <a class="hover:underline" href="/terms-and-conditions">Terms and Conditions</a> • <a class="hover:underline" href="/privacy-policy">Privacy Policy</a></p>
            </div>
        </div>
    </div>
