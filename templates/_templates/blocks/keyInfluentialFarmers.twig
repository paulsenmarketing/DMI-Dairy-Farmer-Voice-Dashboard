<div class="w-full px-8 bg-brandWhiteBlue py-12 lg:py-20">
    <div class="block-container">
        <div class="flex flex-wrap">
            <div class="lg:w-[47%] border-[8px] border-brandLightBlue bg-white relative">
                <div class="p-12 mt-6 text-center">
                    <h3 class="text-[50px] font-bold mb-4 text-center">{{ block.text }}</h3>
                    <div class="lg:w-96 mx-auto">{{ block.richText50 }}</div>
                </div>
            </div>
            <div class="lg:w-[47%] border-[8px] border-brandLightGreen bg-white relative max-lg:mt-12 ml-0 lg:ml-10">
                <div class="p-12 mt-6 text-center">
                    <h3 class="text-[50px] font-bold mb-4 text-center">{{ block.text2 }}</h3>
                    <div class="lg:w-96 mx-auto">{{ block.richText502 }}</div>
                </div>
            </div>
        </div>
    </div>
</div>